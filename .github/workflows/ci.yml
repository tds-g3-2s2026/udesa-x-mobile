name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: tds-g3-2s2026/udesa-x-platform/.github/workflows/ci-node.yml@main
    secrets: inherit
    # mobile ships through Expo and deploys no container, so there is no
    # production image to build and check. The gate stays off until S5, per
    # ARQUITECTURA.md.
